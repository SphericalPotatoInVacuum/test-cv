name: Check

on: [push]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Compile .tex
        uses: ./
        with:
          filename: 'CV/Bogdanov.tex'
      - name: View directory
        run: |
          echo $(pwd)
          echo $(ls)
